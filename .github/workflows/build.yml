name: Build and Publish

on:
    release:
        types: [created]

jobs:
    build-and-publish:
        runs-on: ubuntu-latest
        permissions:
            id-token: write
            contents: read

        steps:
            - name: Checkout
              uses: actions/checkout@v4

            - name: Setup Node.js
              uses: actions/setup-node@v4
              with:
                  node-version: '22'
                  registry-url: 'https://registry.npmjs.org/'
                  scope: '@icemobile'
                  always-auth: true
                  token: ${{ secrets.NPM_TOKEN }}

            - name: Start ssh-agent
              uses: webfactory/ssh-agent@v0.5.4
              with:
                  ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

            - name: Debug npm auth
              run: npm whoami || echo "Failed auth"
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

            - name: Install dependencies
              run: npm ci
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}

            - name: Run tests
              run: npm test

            - name: Authenticate for publish
              run: |
                  echo "//registry.npmjs.org/:_authToken=${NODE_AUTH_TOKEN}" > $NPM_CONFIG_USERCONFIG
              env:
                  NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
                  NPM_CONFIG_USERCONFIG: /home/runner/.npmrc

            - name: Publish to npm
              if: success()
              run: |
                  npm config set //registry.npmjs.org/:_authToken=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
                  npm publish --access=restricted
