'use strict';

const { expect } = require('chai');
const config = require('../config');

// Will load topology from string and parse to JSON
describe('Validate config', () => {

    it('Loads config JSON from environmental variable', async function (done) {

    });

    it('Handles JSON errors loading config', async function (done) {

    });

    it('Handles missing keys in configuration', async function (done) {

    });
});